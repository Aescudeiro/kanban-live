# PostgreSQL connection used by the API (NestJS) and Drizzle Kit.
# Defaults match the docker-compose Postgres service at the repo root.
DATABASE_URL=postgres://kanban:kanban@localhost:5432/kanban_live
